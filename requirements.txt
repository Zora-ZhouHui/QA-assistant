# Web 框架
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic-settings>=2.2       # .env 配置读取

# 大模型（走 OpenAI 兼容接口，当前接 DeepSeek）
openai>=1.30

# RAG：本地 embedding 模型 + 向量库
sentence-transformers>=3.0   # 首次运行会自动下载 bge 模型（约 100MB）
chromadb>=0.5                # 本地嵌入式向量库

# 文档解析
beautifulsoup4>=4.12         # HTML

# FAQ 爬虫（虽然 openai 已传递安装，显式声明更稳）
httpx>=0.27
